import { Button } from '@/components/ui/button'
import React from 'react'

const Header = () => {
  return (
    <div>
      <Button size={'lg'}>Header </Button>
    </div>
  )
}

export default Header
